plugins {
    id "com.gradle.develocity" version "4.4.0"
}
buildCache {
    local {
        enabled = true
    }
}
develocity {
    buildScan {
        termsOfUseAgree = 'yes'
        termsOfUseUrl = 'https://gradle.com/help/legal-terms-of-use'
    }
}